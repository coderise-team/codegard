import EmptyState from './EmptyState';

/**
 * DailyChallenge — daily problem + streak tracker.
 *
 * STUB: there's no daily-challenge / streak endpoint on the backend yet.
 * Shown as a placeholder until that lands; remove this stub then.
 */
export default function DailyChallenge() {
  return (
    <EmptyState
      icon="flame"
      title="Daily challenge"
      sub="Coming soon — daily problems and streaks aren't available yet."
    />
  );
}
